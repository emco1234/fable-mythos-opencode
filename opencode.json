{
  "$schema": "https://opencode.ai/config.json",
  "_comment": "Snippet to merge into your ~/.config/opencode/opencode.json. The 5 agents are auto-discovered from agents/*.md — do NOT add them to the agent block (causes ConfigInvalidError). Only add AGENTS.md to instructions.",
  "instructions": [
    "~/.config/opencode/AGENTS.md"
  ]
}